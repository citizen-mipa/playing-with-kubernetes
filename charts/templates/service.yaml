apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - protocol: {{ .Values.pod.protocol }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.pod.port }}